# Current

Solar revenue workspace — dialer, roof design, map, offers, proof, and local AI studio.

Run it locally:

```bash
npm install
npx prisma db push
npm run dev
```

Open [http://localhost:6793](http://localhost:6793).
