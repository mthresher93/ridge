(() => {
  'use strict';
  const DB_KEY='conduit.crm.v1';
  const DIALER_KEY='conduitDialerV1';
  const STAGES=['New Lead','Attempting Contact','Contacted','Qualified','Promising Callback','Appointment Set','Appointment Confirmed','Appointment Sat','Proposal','Contract Sent','Contract Signed','Site Survey','Design','Permitting','Installation Scheduled','Installed','PTO','Closed Won','Closed Lost','Cancelled','Prospect'];
  const now=()=>new Date().toISOString();
  const id=prefix=>`${prefix}-${crypto.randomUUID?.()||Date.now()+'-'+Math.random().toString(16).slice(2)}`;
  const esc=value=>String(value??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
  const money=value=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(Number(value)||0);
  const emptyDB=()=>({version:2,leads:[],opportunities:[],callbacks:[],appointments:[],tasks:[],kpiEvents:[],agentAudit:[],imports:[],activities:[],updatedAt:now()});
  const offset=(days,hour=10)=>{const d=new Date();d.setDate(d.getDate()+days);d.setHours(hour,0,0,0);return d.toISOString()};
  function starterDB(){const created=now(),leads=[
    {id:'starter-lead-1',name:'Jordan Lee',company:'BrightPeak Solar',phone:'+15550101001',email:'jordan@example.test',status:'Qualified',priority:'High',owner:'Michael',source:'Website form',sourceUrl:'',consent:'verified',consentSource:'Starter workspace consent record',dnc:false,attempts:2,nextAction:'Confirm site survey availability',nextFollowUp:offset(0,11),estimatedValue:18500,notes:'Starter workspace record — replace with an actual lead.',createdAt:created,updatedAt:created,starter:true},
    {id:'starter-lead-2',name:'Casey Morgan',company:'Morgan Residence',phone:'+15550101002',email:'casey@example.test',status:'Promising Callback',priority:'Critical',owner:'Michael',source:'Referral',sourceUrl:'',consent:'verified',consentSource:'Starter workspace consent record',dnc:false,attempts:3,nextAction:'Review utility bill and schedule consultation',nextFollowUp:offset(0,14),estimatedValue:24000,notes:'Starter workspace record — callback due today.',createdAt:created,updatedAt:created,starter:true},
    {id:'starter-lead-3',name:'Taylor Brooks',company:'Brooks Family',phone:'+15550101003',email:'taylor@example.test',status:'Appointment Set',priority:'High',owner:'Avery',source:'Google Ads',sourceUrl:'',consent:'verified',consentSource:'Starter workspace consent record',dnc:false,attempts:1,nextAction:'Prepare consultation notes',nextFollowUp:offset(1,9),estimatedValue:32000,notes:'Starter workspace record — appointment scheduled.',createdAt:created,updatedAt:created,starter:true},
    {id:'starter-lead-4',name:'Riley Chen',company:'Chen Property Group',phone:'+15550101004',email:'riley@example.test',status:'Proposal',priority:'Medium',owner:'Avery',source:'Partner',sourceUrl:'',consent:'verified',consentSource:'Starter workspace consent record',dnc:false,attempts:4,nextAction:'Follow up on proposal questions',nextFollowUp:offset(-1,15),estimatedValue:46500,notes:'Starter workspace record — overdue follow-up.',createdAt:created,updatedAt:created,starter:true},
    {id:'starter-lead-5',name:'Sam Rivera',company:'Rivera Home',phone:'+15550101005',email:'sam@example.test',status:'New Lead',priority:'Medium',owner:'Unassigned',source:'Organic',sourceUrl:'',consent:'unknown',consentSource:'',dnc:false,attempts:0,nextAction:'Verify consent before calling',nextFollowUp:offset(2,10),estimatedValue:12800,notes:'Starter workspace record — calling remains blocked pending consent.',createdAt:created,updatedAt:created,starter:true}
  ];const opportunities=leads.slice(0,4).map((lead,index)=>({id:`starter-opp-${index+1}`,leadId:lead.id,name:`${lead.name} · Solar project`,company:lead.company,stage:lead.status,value:lead.estimatedValue,owner:lead.owner,source:lead.source,nextAction:lead.nextAction,createdAt:created,updatedAt:created,stageEnteredAt:offset(-index-1),history:[],starter:true}));return{version:2,leads,opportunities,callbacks:[{id:'starter-callback-1',leadId:'starter-lead-2',type:'promising',dueAt:offset(0,14),reason:'Review utility bill and qualification questions',assignedUser:'Michael',notes:'Starter callback — no external call has occurred.',status:'open',createdAt:created,starter:true},{id:'starter-callback-2',leadId:'starter-lead-4',type:'follow-up',dueAt:offset(-1,15),reason:'Proposal questions remain open',assignedUser:'Avery',notes:'Starter callback — overdue for workflow demonstration.',status:'open',createdAt:created,starter:true},{id:'starter-callback-3',leadId:'starter-lead-3',type:'confirmation',dueAt:offset(1,8),reason:'Confirm tomorrow’s consultation',assignedUser:'Avery',notes:'Starter callback linked to appointment.',status:'open',createdAt:created,starter:true}],appointments:[{id:'starter-appt-1',leadId:'starter-lead-3',type:'Solar consultation',startsAt:offset(1,10),duration:45,setter:'Avery',closer:'Michael',location:'Video call',status:'scheduled',notes:'Starter appointment — not a real booking.',createdAt:created,starter:true},{id:'starter-appt-2',leadId:'starter-lead-1',type:'Site survey',startsAt:offset(2,13),duration:60,setter:'Michael',closer:'Michael',location:'Customer site',status:'scheduled',notes:'Starter appointment — not a real booking.',createdAt:created,starter:true}],tasks:[{id:'starter-task-1',leadId:'starter-lead-1',title:'Confirm site survey availability',dueAt:offset(0,11),status:'open',createdAt:created,starter:true},{id:'starter-task-2',leadId:'starter-lead-4',title:'Review proposal questions',dueAt:offset(-1,15),status:'open',createdAt:created,starter:true}],kpiEvents:[{id:'starter-kpi-1',type:'dial_attempt',leadId:'starter-lead-1',at:offset(-3),classification:'STARTER DATA'},{id:'starter-kpi-2',type:'connected_call',leadId:'starter-lead-1',at:offset(-3),classification:'STARTER DATA'},{id:'starter-kpi-3',type:'qualified_lead',leadId:'starter-lead-1',at:offset(-2),classification:'STARTER DATA'},{id:'starter-kpi-4',type:'appointment_set',leadId:'starter-lead-3',appointmentId:'starter-appt-1',at:offset(-1),classification:'STARTER DATA'}],agentAudit:[],imports:[{id:'starter-import',source:'Conduit starter workspace',at:created,imported:5,rejected:0,total:5}],activities:[{id:'starter-activity',entityType:'workspace',entityId:'starter',type:'starter_loaded',detail:'Clearly labeled local starter records loaded; no external activity occurred.',at:created}],starterWorkspace:true,updatedAt:created}}
  function normalizeStarter(db){for(const item of db.callbacks||[])if(item.type==='follow-up')item.type='standard';return db}
  function load(){try{const stored=localStorage.getItem(DB_KEY);if(!stored){const starter=normalizeStarter(starterDB());localStorage.setItem(DB_KEY,JSON.stringify(starter));return starter}const parsed={...emptyDB(),...JSON.parse(stored)},hasRecords=['leads','opportunities','callbacks','appointments','tasks','kpiEvents'].some(key=>parsed[key]?.length);if(!hasRecords&&!parsed.imports?.length&&!parsed.activities?.length){const starter=normalizeStarter(starterDB());localStorage.setItem(DB_KEY,JSON.stringify(starter));return starter}return parsed}catch{return emptyDB()}}
  function canonicalLeadId(value){try{return JSON.parse(localStorage.getItem(DIALER_KEY)||'{}').leads?.find(x=>x.id===value)?.crmLeadId||value}catch{return value}}
  function save(db){for(const event of db.kpiEvents||[])event.leadId=canonicalLeadId(event.leadId);for(const opp of db.opportunities||[]){const latest=opp.history?.[0];if(latest?.source==='appointment'&&latest.from===latest.to&&latest.to==='Appointment Set')latest.from='New Lead'}const booked=(db.kpiEvents||[]).filter(x=>x.type==='appointment_set'&&x.appointmentId);if(booked.length)db.kpiEvents=(db.kpiEvents||[]).filter(event=>!booked.some(item=>event!==item&&event.type==='appointment_set'&&event.leadId===item.leadId&&!event.appointmentId&&Math.abs(Date.parse(event.at)-Date.parse(item.at))<3600000));db.updatedAt=now();localStorage.setItem(DB_KEY,JSON.stringify(db));window.dispatchEvent(new CustomEvent('conduit:crm-updated'));}
  function activity(db,entityType,entityId,type,detail){db.activities.unshift({id:id('activity'),entityType,entityId,type,detail,at:now()});db.activities=db.activities.slice(0,500)}
  function normalizePhone(value){return String(value||'').replace(/[\s().-]/g,'')}
  function validPhone(value){return /^\+[1-9]\d{7,14}$/.test(value)}
  function leadScore(lead){let score=0;if(lead.phone)score+=20;if(lead.email)score+=15;if(lead.company)score+=15;if(lead.consent==='verified')score+=20;if(lead.sourceUrl)score+=15;if(lead.nextAction)score+=10;if(lead.dnc)score=0;return Math.min(100,score)}
  function leadEligibility(lead){if(lead.dnc)return['bad','DNC'];if(lead.consent==='missing')return['bad','No consent'];if(!validPhone(lead.phone))return['warn','No valid phone'];return['ok','Eligible']}
  function addToDialer(lead){const state={leads:[],events:[],attempts:0,reached:0,activeId:null,provider:{ready:false,activeProvider:'unknown',providers:[]},acceptedCall:null,...JSON.parse(localStorage.getItem(DIALER_KEY)||'{}')};if(state.leads.some(item=>item.crmLeadId===lead.id||item.phone===lead.phone))return false;state.leads.push({id:id('dialer-lead'),crmLeadId:lead.id,name:lead.name,company:lead.company,phone:lead.phone,consent:lead.consent,priority:lead.priority,dnc:lead.dnc,stage:lead.status||'Prospect',notes:lead.notes||'',callbackAt:'',createdAt:now()});state.activeId=state.activeId||state.leads[0].id;state.events.unshift({id:id('evt'),type:'crm',detail:`${lead.name} added from CRM`,leadId:state.leads.at(-1).id,at:now()});localStorage.setItem(DIALER_KEY,JSON.stringify(state));window.dispatchEvent(new CustomEvent('conduit:dialer-updated'));return true}
  function ensureOpportunity(db,lead){let opp=db.opportunities.find(item=>item.leadId===lead.id);if(!opp){const initialStage=lead.status==='Appointment Set'?'New Lead':STAGES.includes(lead.status)?lead.status:'New Lead';opp={id:id('opp'),leadId:lead.id,name:lead.name,company:lead.company,stage:initialStage,value:Number(lead.estimatedValue)||0,owner:lead.owner||'Unassigned',source:lead.source||'Manual',nextAction:lead.nextAction||'',createdAt:now(),updatedAt:now(),stageEnteredAt:now(),history:[]};db.opportunities.unshift(opp);activity(db,'opportunity',opp.id,'created','Opportunity created from lead')}return opp}
  let leadUI={query:'',status:'all',source:'all',selectedId:null,importOpen:false};
  function leadPage(){return document.querySelector('.page[data-page="leads"] .body')}
  function renderLeads(){const root=leadPage();if(!root)return;const db=load(),sources=[...new Set(db.leads.map(x=>x.source).filter(Boolean))];const leads=db.leads.filter(lead=>(leadUI.status==='all'||lead.status===leadUI.status)&&(leadUI.source==='all'||lead.source===leadUI.source)&&(!leadUI.query||[lead.name,lead.company,lead.phone,lead.email,lead.owner].join(' ').toLowerCase().includes(leadUI.query.toLowerCase())));const selected=db.leads.find(x=>x.id===leadUI.selectedId);
    root.innerHTML=`<div class="crm-workspace"><div class="crm-toolbar"><input class="crm-search" id="crmLeadSearch" value="${esc(leadUI.query)}" placeholder="Search leads, companies, phones, email, owner"><select id="crmLeadStatus"><option value="all">All stages</option>${STAGES.map(x=>`<option ${leadUI.status===x?'selected':''}>${x}</option>`).join('')}</select><select id="crmLeadSource"><option value="all">All sources</option>${sources.map(x=>`<option ${leadUI.source===x?'selected':''}>${esc(x)}</option>`).join('')}</select><span class="crm-spacer"></span><button class="crm-btn" id="crmImportToggle">Import CSV</button><button class="crm-btn primary" id="crmAddLead">Add lead</button></div>${leadUI.importOpen?importPanel():''}<div class="crm-metrics">${metric('Total leads',db.leads.length,'Persisted CRM records')}${metric('Eligible',db.leads.filter(x=>leadEligibility(x)[1]==='Eligible').length,'Callable after review')}${metric('DNC',db.leads.filter(x=>x.dnc).length,'Suppressed')}${metric('Due follow-ups',db.leads.filter(x=>x.nextFollowUp&&Date.parse(x.nextFollowUp)<=Date.now()).length,'Needs action')}${metric('Pipeline value',money(db.leads.reduce((s,x)=>s+(Number(x.estimatedValue)||0),0)),'Estimated, not contracted')}</div>${leads.length?leadTable(leads):`<div class="crm-empty"><h3>No real Lead records</h3><p>Add a Lead manually, import a permitted CSV, or connect an approved source. Conduit will not populate this table with demo people.</p></div>`}</div>${selected?leadDrawer(selected,db):''}`;bindLeads();}
  function metric(label,value,helper){return`<div class="crm-metric"><div class="label">${esc(label)}</div><div class="value">${esc(value)}</div><div class="helper">${esc(helper)}</div></div>`}
  function leadTable(leads){return`<div class="crm-table-wrap"><table class="crm-table"><thead><tr><th>Lead</th><th>Phone</th><th>Stage</th><th>Score</th><th>Source</th><th>Consent</th><th>Attempts</th><th>Next action</th><th>Value</th><th>Dialer</th></tr></thead><tbody>${leads.map(lead=>{const [tone,label]=leadEligibility(lead);return`<tr data-lead-row="${lead.id}"><td><strong>${esc(lead.name)}</strong><span class="crm-sub">${esc(lead.company||'No company')} · ${esc(lead.owner||'Unassigned')}</span></td><td>${esc(lead.phone||'—')}</td><td><span class="crm-badge">${esc(lead.status)}</span></td><td>${leadScore(lead)}</td><td>${esc(lead.source||'Manual')}</td><td><span class="crm-badge ${tone}">${esc(label)}</span></td><td>${Number(lead.attempts)||0}</td><td>${esc(lead.nextAction||'—')}</td><td>${money(lead.estimatedValue)}</td><td><button class="crm-btn" data-add-dialer="${lead.id}" ${tone!=='ok'?'disabled':''}>Add</button></td></tr>`}).join('')}</tbody></table></div>`}
  function importPanel(){return`<section class="crm-import"><strong>CSV import · local validation</strong><p>Required: name. Recommended: company, phone, email, source, consent, dnc, owner, estimatedValue. Telephone numbers must use E.164 format.</p><textarea id="crmCsvInput" placeholder="name,company,phone,email,source,consent,dnc\nJane Doe,Example Co,+15551234567,jane@example.com,User CSV,verified,false"></textarea><div class="crm-actions"><button class="crm-btn primary" id="crmRunImport">Validate and import</button><button class="crm-btn" id="crmCancelImport">Cancel</button></div><div class="crm-errors" id="crmImportErrors"></div></section>`}
  function leadDrawer(lead,db){const events=db.activities.filter(x=>x.entityType==='lead'&&x.entityId===lead.id).slice(0,12);return`<div class="crm-overlay" id="crmLeadOverlay"><aside class="crm-drawer" role="dialog" aria-modal="true" aria-label="Lead details"><div class="crm-drawer-head"><div><h2>${esc(lead.name)}</h2><p>${esc(lead.company||'No company')} · updated ${new Date(lead.updatedAt).toLocaleString()}</p></div><button class="crm-btn" id="crmCloseLead">Close</button></div><form id="crmLeadForm"><div class="crm-grid">${field('Name','name',lead.name,true)}${field('Company','company',lead.company)}${field('Phone · E.164','phone',lead.phone)}${field('Email','email',lead.email,'email')}${selectField('Stage','status',lead.status,STAGES)}${selectField('Priority','priority',lead.priority,['Low','Medium','High','Critical'])}${field('Owner','owner',lead.owner)}${field('Estimated value','estimatedValue',lead.estimatedValue,'number')}${field('Source','source',lead.source)}${field('Source URL','sourceUrl',lead.sourceUrl,'url')}${selectField('Consent','consent',lead.consent,['unknown','verified','missing'])}${field('Consent source','consentSource',lead.consentSource)}${field('Next follow-up','nextFollowUp',lead.nextFollowUp,'datetime-local')}${field('Next action','nextAction',lead.nextAction)}<div class="crm-field full"><label><input type="checkbox" name="dnc" ${lead.dnc?'checked':''}> Internal DNC suppression</label></div><div class="crm-field full"><label>Notes</label><textarea name="notes">${esc(lead.notes||'')}</textarea></div></div><div class="crm-actions"><button class="crm-btn primary" type="submit">Save changes</button><button class="crm-btn" type="button" id="crmLeadToDialer" ${leadEligibility(lead)[0]!=='ok'?'disabled':''}>Add to Dialer</button><button class="crm-btn" type="button" id="crmLeadOpportunity">Create opportunity</button><button class="crm-btn danger" type="button" id="crmDeleteLead">Delete</button></div></form><section style="margin-top:22px"><h3 style="font-size:18px">Activity timeline</h3>${events.length?events.map(x=>`<div class="dialer-event"><strong>${esc(x.type)}</strong> · ${esc(x.detail)}<time>${new Date(x.at).toLocaleString()}</time></div>`).join(''):'<div class="crm-empty" style="margin-top:10px;padding:22px">No activity recorded.</div>'}</section></aside></div>`}
  function field(label,name,value,type='text'){if(name==='meetingLink'&&value==='url'&&type==='text'){value='';type='url'}return`<div class="crm-field"><label>${esc(label)}</label><input name="${name}" type="${type===true?'text':type}" value="${esc(value||'')}" ${type===true?'required':''}></div>`}
  function selectField(label,name,value,options){return`<div class="crm-field"><label>${esc(label)}</label><select name="${name}">${options.map(x=>`<option value="${esc(x)}" ${x===value?'selected':''}>${esc(x)}</option>`).join('')}</select></div>`}
  function bindLeads(){document.getElementById('crmLeadSearch')?.addEventListener('input',e=>{leadUI.query=e.target.value;renderLeads()});document.getElementById('crmLeadStatus')?.addEventListener('change',e=>{leadUI.status=e.target.value;renderLeads()});document.getElementById('crmLeadSource')?.addEventListener('change',e=>{leadUI.source=e.target.value;renderLeads()});document.getElementById('crmImportToggle')?.addEventListener('click',()=>{leadUI.importOpen=!leadUI.importOpen;renderLeads()});document.getElementById('crmCancelImport')?.addEventListener('click',()=>{leadUI.importOpen=false;renderLeads()});document.getElementById('crmAddLead')?.addEventListener('click',()=>{const db=load(),lead={id:id('lead'),name:'',company:'',phone:'',email:'',status:'Prospect',priority:'Medium',owner:'',source:'Manual',sourceUrl:'',consent:'unknown',consentSource:'',dnc:false,attempts:0,nextAction:'',nextFollowUp:'',estimatedValue:0,notes:'',createdAt:now(),updatedAt:now()};db.leads.unshift(lead);activity(db,'lead',lead.id,'created','Lead record created manually');save(db);leadUI.selectedId=lead.id;renderLeads()});document.querySelectorAll('[data-lead-row]').forEach(row=>row.addEventListener('click',()=>{leadUI.selectedId=row.dataset.leadRow;renderLeads()}));document.querySelectorAll('[data-add-dialer]').forEach(button=>button.addEventListener('click',e=>{e.stopPropagation();const db=load(),lead=db.leads.find(x=>x.id===button.dataset.addDialer);if(lead&&addToDialer(lead)){activity(db,'lead',lead.id,'dialer','Added to Dialer queue');save(db);button.textContent='Added';button.disabled=true}}));document.getElementById('crmCloseLead')?.addEventListener('click',()=>{leadUI.selectedId=null;renderLeads()});document.getElementById('crmLeadOverlay')?.addEventListener('click',e=>{if(e.target.id==='crmLeadOverlay'){leadUI.selectedId=null;renderLeads()}});document.getElementById('crmLeadForm')?.addEventListener('submit',saveLeadForm);document.getElementById('crmLeadToDialer')?.addEventListener('click',()=>{const db=load(),lead=db.leads.find(x=>x.id===leadUI.selectedId);if(lead&&addToDialer(lead)){activity(db,'lead',lead.id,'dialer','Added to Dialer queue');save(db);renderLeads()}});document.getElementById('crmLeadOpportunity')?.addEventListener('click',()=>{const db=load(),lead=db.leads.find(x=>x.id===leadUI.selectedId);if(lead){ensureOpportunity(db,lead);save(db);renderLeads()}});document.getElementById('crmDeleteLead')?.addEventListener('click',()=>{if(!confirm('Delete this Lead record?'))return;const db=load();db.leads=db.leads.filter(x=>x.id!==leadUI.selectedId);activity(db,'lead',leadUI.selectedId,'deleted','Lead record deleted');save(db);leadUI.selectedId=null;renderLeads()});document.getElementById('crmRunImport')?.addEventListener('click',runCsvImport)}
  function saveLeadForm(e){e.preventDefault();const db=load(),lead=db.leads.find(x=>x.id===leadUI.selectedId);if(!lead)return;const data=new FormData(e.target),previous=lead.status;['name','company','phone','email','status','priority','owner','source','sourceUrl','consent','consentSource','nextAction','nextFollowUp','notes'].forEach(key=>lead[key]=String(data.get(key)||'').trim());lead.phone=normalizePhone(lead.phone);lead.estimatedValue=Number(data.get('estimatedValue'))||0;lead.dnc=data.get('dnc')==='on';lead.updatedAt=now();activity(db,'lead',lead.id,'updated',previous===lead.status?'Lead fields updated':`Stage changed ${previous} → ${lead.status}`);const opp=db.opportunities.find(x=>x.leadId===lead.id);if(opp&&previous!==lead.status){opp.stage=lead.status;opp.updatedAt=now();opp.stageEnteredAt=now();opp.history.unshift({from:previous,to:lead.status,at:now(),source:'lead'})}save(db);renderLeads()}
  function parseCsv(text){const lines=text.trim().split(/\r?\n/).filter(Boolean),split=line=>line.match(/("[^"]*(?:""[^"]*)*"|[^,]*)(?:,|$)/g)?.map(x=>x.replace(/,$/,'').replace(/^"|"$/g,'').replace(/""/g,'"'))||[];if(lines.length<2)throw new Error('CSV requires a header and at least one data row.');const headers=split(lines[0]).map(x=>x.trim());return lines.slice(1).map((line,index)=>({row:index+2,values:Object.fromEntries(headers.map((h,i)=>[h,split(line)[i]?.trim()||'']))}))}
  function runCsvImport(){const box=document.getElementById('crmImportErrors');try{const rows=parseCsv(document.getElementById('crmCsvInput').value),db=load(),errors=[],accepted=[];for(const row of rows){const v=row.values,name=v.name||v.full_name;if(!name){errors.push(`Row ${row.row}: name is required`);continue}const phone=normalizePhone(v.phone);if(phone&&!validPhone(phone)){errors.push(`Row ${row.row}: invalid E.164 phone`);continue}if((phone&&db.leads.some(x=>x.phone===phone))||(v.email&&db.leads.some(x=>x.email.toLowerCase()===v.email.toLowerCase()))){errors.push(`Row ${row.row}: duplicate phone or email`);continue}accepted.push({id:id('lead'),name,company:v.company||'',phone,email:v.email||'',status:STAGES.includes(v.status)?v.status:'Prospect',priority:v.priority||'Medium',owner:v.owner||'',source:v.source||'User CSV',sourceUrl:v.sourceUrl||'',consent:['verified','missing'].includes(v.consent)?v.consent:'unknown',consentSource:v.consentSource||'',dnc:/^(true|1|yes)$/i.test(v.dnc||''),attempts:0,nextAction:v.nextAction||'',nextFollowUp:v.nextFollowUp||'',estimatedValue:Number(v.estimatedValue)||0,notes:v.notes||'',createdAt:now(),updatedAt:now()})}db.leads.unshift(...accepted);db.imports.unshift({id:id('import'),source:'User CSV',at:now(),imported:accepted.length,rejected:errors.length,total:rows.length});accepted.forEach(lead=>activity(db,'lead',lead.id,'imported','Imported from user CSV'));save(db);box.textContent=`Imported ${accepted.length}; rejected ${errors.length}.${errors.length?'\n'+errors.join('\n'):''}`;if(accepted.length)setTimeout(()=>{leadUI.importOpen=false;renderLeads()},800)}catch(error){box.textContent=error.message}}
  const PIPELINE_GROUPS=[
    {id:'new',number:'01',label:'New & Contact',drop:'New Lead',stages:['New Lead','Prospect','Attempting Contact','Contacted']},
    {id:'qualified',number:'02',label:'Qualified',drop:'Qualified',stages:['Qualified','Promising Callback']},
    {id:'appointment',number:'03',label:'Appointments',drop:'Appointment Set',stages:['Appointment Set','Appointment Confirmed','Appointment Sat']},
    {id:'proposal',number:'04',label:'Proposal & Contract',drop:'Proposal',stages:['Proposal','Contract Sent','Contract Signed']},
    {id:'project',number:'05',label:'Solar Project',drop:'Site Survey',stages:['Site Survey','Design','Permitting','Installation Scheduled','Installed','PTO']},
    {id:'closed',number:'06',label:'Closed',drop:'Closed Won',stages:['Closed Won','Closed Lost','Cancelled']}
  ];
  let pipelineUI={view:'board',selectedId:null,owner:'all',source:'all',urgency:'all'};
  function pipelinePage(){return document.querySelector('.page[data-page="pipeline"] .body')}
  function opportunityUrgency(x){
    if(String(x.stage).startsWith('Closed'))return'healthy';
    const age=daysInStage(x),close=x.expectedClose?Date.parse(`${x.expectedClose}T23:59:59`):NaN;
    if(!x.nextAction||age>=14||(Number.isFinite(close)&&close<Date.now()))return'critical';
    if(age>=7)return'attention';
    return'healthy';
  }
  function renderPipeline(){
    const root=pipelinePage();if(!root)return;
    const db=load(),owners=[...new Set(db.opportunities.map(x=>x.owner).filter(Boolean))],sources=[...new Set(db.opportunities.map(x=>x.source).filter(Boolean))];
    const scoped=db.opportunities.filter(x=>(pipelineUI.owner==='all'||x.owner===pipelineUI.owner)&&(pipelineUI.source==='all'||x.source===pipelineUI.source));
    const items=scoped.filter(x=>pipelineUI.urgency==='all'||opportunityUrgency(x)===pipelineUI.urgency),selected=db.opportunities.find(x=>x.id===pipelineUI.selectedId);
    const open=scoped.filter(x=>!String(x.stage).startsWith('Closed')),weighted=open.reduce((sum,x)=>sum+(+x.value||0)*(+x.probability||0)/100,0),counts={critical:0,attention:0,healthy:0};open.forEach(x=>counts[opportunityUrgency(x)]++);
    root.innerHTML=`<div class="crm-workspace pipeline-structured">
      <section class="pipeline-command-bar"><div><span class="pipeline-eyebrow">SOLAR REVENUE PIPELINE</span><h2>Move the right deals first</h2><p>Six lifecycle phases organize every detailed CRM stage. Color reflects time in stage and next-action readiness.</p></div><button class="crm-btn primary" id="pipelineAdd">+ New opportunity</button></section>
      <div class="crm-toolbar pipeline-toolbar"><div class="crm-views" aria-label="Pipeline view">${['board','table','summary'].map(x=>`<button class="crm-btn ${pipelineUI.view===x?'active':''}" data-pipeline-view="${x}">${x[0].toUpperCase()+x.slice(1)}</button>`).join('')}</div><span class="pipeline-toolbar-divider"></span><label><span>Owner</span><select id="pipelineOwner"><option value="all">All owners</option>${owners.map(x=>`<option ${pipelineUI.owner===x?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label><span>Source</span><select id="pipelineSource"><option value="all">All sources</option>${sources.map(x=>`<option ${pipelineUI.source===x?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label><span>Urgency</span><select id="pipelineUrgency"><option value="all">All urgency</option><option value="critical" ${pipelineUI.urgency==='critical'?'selected':''}>Critical</option><option value="attention" ${pipelineUI.urgency==='attention'?'selected':''}>Needs attention</option><option value="healthy" ${pipelineUI.urgency==='healthy'?'selected':''}>On track</option></select></label></div>
      <div class="pipeline-health-metrics"><article class="forecast"><span>Open pipeline</span><b>${money(open.reduce((sum,x)=>sum+(+x.value||0),0))}</b><small>${open.length} open · ${money(weighted)} weighted</small></article><button class="critical ${pipelineUI.urgency==='critical'?'active':''}" data-pipeline-urgency-card="critical"><span>Critical</span><b>${counts.critical}</b><small>14+ days or action missing</small></button><button class="attention ${pipelineUI.urgency==='attention'?'active':''}" data-pipeline-urgency-card="attention"><span>Needs attention</span><b>${counts.attention}</b><small>7–13 days in stage</small></button><button class="healthy ${pipelineUI.urgency==='healthy'?'active':''}" data-pipeline-urgency-card="healthy"><span>On track</span><b>${counts.healthy}</b><small>Fresh with a next action</small></button><article class="coverage"><span>Next-action coverage</span><b>${Math.round(open.filter(x=>x.nextAction).length/Math.max(1,open.length)*100)}%</b><small>${open.filter(x=>x.nextAction).length}/${open.length} open deals ready</small></article></div>
      ${pipelineCalendar(db)}
      <div class="pipeline-legend"><button class="${pipelineUI.urgency==='all'?'active':''}" data-pipeline-urgency-card="all">Show all</button><span><i class="critical"></i>Critical</span><span><i class="attention"></i>Needs attention</span><span><i class="healthy"></i>On track</span><em>Drag a card to another lifecycle phase; use the drawer for a specific substage.</em></div>
      ${pipelineContent(items)}
    </div>${selected?opportunityDrawer(selected,db):''}`;bindPipeline()
  }
  function pipelineContent(items){
    if(!items.length)return`<div class="crm-empty pipeline-empty"><h3>No opportunities match this view</h3><p>Clear an urgency, owner, or source filter—or create a new residential solar opportunity.</p><button class="crm-btn" data-pipeline-urgency-card="all">Clear urgency filter</button></div>`;
    if(pipelineUI.view==='table')return`<div class="crm-table-wrap pipeline-table-wrap"><table class="crm-table pipeline-table"><thead><tr><th>Urgency</th><th>Homeowner opportunity</th><th>Phase / stage</th><th>Value</th><th>Probability</th><th>Owner</th><th>Age</th><th>Next action</th></tr></thead><tbody>${items.map(x=>{const urgency=opportunityUrgency(x),group=PIPELINE_GROUPS.find(g=>g.stages.includes(x.stage));return`<tr class="urgency-${urgency}" data-opp-row="${x.id}"><td><span class="pipeline-urgency ${urgency}">${urgency==='critical'?'Critical':urgency==='attention'?'Attention':'On track'}</span></td><td><strong>${esc(String(x.name).replace(/ · Solar project$/,''))}</strong><span class="crm-sub">${esc(x.company||'Property not set')}</span></td><td><span class="pipeline-phase-text">${esc(group?.label||'Other')}</span><small>${esc(x.stage)}</small></td><td><strong>${money(x.value)}</strong></td><td>${Number(x.probability)||0}%</td><td>${esc(x.owner||'Unassigned')}</td><td><b class="pipeline-age ${urgency}">${daysInStage(x)}d</b></td><td>${esc(x.nextAction||'Next action required')}</td></tr>`}).join('')}</tbody></table></div>`;
    if(pipelineUI.view==='summary')return`<div class="pipeline-summary structured">${PIPELINE_GROUPS.map(group=>{const rows=items.filter(x=>group.stages.includes(x.stage)),value=rows.reduce((sum,x)=>sum+(+x.value||0),0),weighted=rows.reduce((sum,x)=>sum+(+x.value||0)*(+x.probability||0)/100,0),critical=rows.filter(x=>opportunityUrgency(x)==='critical').length;return`<button class="pipeline-summary-card" data-stage-summary="${group.id}"><span class="pipeline-phase-number">${group.number}</span><h3>${group.label}</h3><div class="big">${rows.length}</div><p>${money(value)} total · ${money(weighted)} weighted</p><div class="pipeline-summary-health"><b class="${critical?'critical':'healthy'}">${critical?`${critical} critical`:'Healthy'}</b><span>${group.stages.map(stage=>`${stage}: ${rows.filter(x=>x.stage===stage).length}`).join(' · ')}</span></div></button>`}).join('')}</div>`;
    return`<div class="pipeline-board structured" aria-label="Residential solar opportunity pipeline. Drag cards between lifecycle phases.">${PIPELINE_GROUPS.map(group=>{const rows=items.filter(x=>group.stages.includes(x.stage)),value=rows.reduce((sum,x)=>sum+(+x.value||0),0),weighted=rows.reduce((sum,x)=>sum+(+x.value||0)*(+x.probability||0)/100,0);return`<section class="pipeline-column phase-${group.id}" data-stage-drop="${esc(group.drop)}"><div class="pipeline-column-head"><div><span class="pipeline-phase-number">${group.number}</span><div><h3>${esc(group.label)}</h3><p>${rows.length} deal${rows.length===1?'':'s'} · ${money(value)}</p></div></div><small>${money(weighted)} weighted</small></div><div class="pipeline-column-body">${rows.length?rows.map(x=>pipelineCard(x)).join(''):`<div class="pipeline-column-empty"><b>No deals here</b><span>Drag a card into this phase</span></div>`}</div></section>`}).join('')}</div>`
  }
  function pipelineCard(x){const urgency=opportunityUrgency(x),age=daysInStage(x),label=urgency==='critical'?'Critical':urgency==='attention'?'Attention':'On track';return`<button draggable="true" class="pipeline-card urgency-${urgency}" data-opp-row="${x.id}"><div class="pipeline-card-top"><span class="pipeline-urgency ${urgency}">${label}</span><span class="pipeline-stage-chip">${esc(x.stage)}</span></div><strong>${esc(String(x.name).replace(/ · Solar project$/,''))}</strong><p class="pipeline-property">${esc(x.company||'Property not set')}</p><div class="pipeline-value-row"><b>${money(x.value)}</b><span>${Number(x.probability)||0}% likely</span></div><div class="pipeline-probability"><i style="width:${Math.max(0,Math.min(100,Number(x.probability)||0))}%"></i></div><div class="pipeline-next"><span>Next action</span><b>${esc(x.nextAction||'Add a next action')}</b></div><div class="pipeline-card-foot"><span><i>${esc((x.owner||'U')[0])}</i>${esc(x.owner||'Unassigned')}</span><span>${esc(x.source||'Manual')}</span><b class="pipeline-age ${urgency}">${age}d</b></div></button>`}
  function pipelineCalendar(db){
    const appointments=(db.appointments||[]).filter(x=>!['cancelled','completed','no-show'].includes(String(x.status).toLowerCase())).sort((a,b)=>Date.parse(a.startsAt)-Date.parse(b.startsAt));
    const now=Date.now(),dayEnd=new Date();dayEnd.setHours(23,59,59,999);const weekEnd=now+7*86400000,upcoming=appointments.filter(x=>Date.parse(x.startsAt)>=now).slice(0,4),today=appointments.filter(x=>Date.parse(x.startsAt)>=now&&Date.parse(x.startsAt)<=dayEnd.getTime()).length,week=appointments.filter(x=>Date.parse(x.startsAt)>=now&&Date.parse(x.startsAt)<weekEnd).length,confirm=appointments.filter(x=>/needs confirmation/i.test(x.status)).length;
    const appointmentCard=x=>{const lead=(db.leads||[]).find(item=>item.id===x.leadId),date=new Date(x.startsAt),tone=/needs confirmation/i.test(x.status)?'attention':/confirmed/i.test(x.status)?'healthy':'scheduled';return`<button class="pipeline-calendar-event ${tone}" data-crm-route="conversations-appointments"><time><b>${date.toLocaleDateString([],{month:'short',day:'numeric'})}</b><span>${date.toLocaleTimeString([],{hour:'numeric',minute:'2-digit'})}</span></time><div><strong>${esc(lead?.name||'Unlinked appointment')}</strong><span>${esc(x.type||'Solar appointment')} · ${esc(x.closer||x.setter||'Unassigned')}</span></div><em>${esc(String(x.status||'scheduled').replaceAll('-',' '))}</em></button>`};
    return`<section class="pipeline-calendar-panel"><header><div><span class="pipeline-eyebrow">CALENDAR · UPCOMING SOLAR APPOINTMENTS</span><h3>Appointments connected to the pipeline</h3></div><div class="pipeline-calendar-stats"><span><b>${today}</b> today</span><span><b>${week}</b> next 7 days</span><span class="${confirm?'attention':'healthy'}"><b>${confirm}</b> need confirmation</span></div><button class="crm-btn" data-crm-route="conversations-appointments">Open full calendar</button></header><div class="pipeline-calendar-strip">${upcoming.length?upcoming.map(appointmentCard).join(''):'<div class="pipeline-calendar-empty"><b>No upcoming appointments</b><span>Book a consultation, site survey, or proposal review from the full calendar.</span></div>'}</div></section>`
  }
  function daysInStage(x){return Math.max(0,Math.floor((Date.now()-Date.parse(x.stageEnteredAt||x.updatedAt||x.createdAt))/86400000))}
  function opportunityDrawer(opp,db){const events=db.activities.filter(x=>x.entityType==='opportunity'&&x.entityId===opp.id).slice(0,12);return`<div class="crm-overlay" id="pipelineOverlay"><aside class="crm-drawer" role="dialog" aria-modal="true"><div class="crm-drawer-head"><div><h2>${esc(opp.name)}</h2><p>${esc(opp.company||'No company')} · ${daysInStage(opp)} days in stage</p></div><button class="crm-btn" id="pipelineClose">Close</button></div><form id="pipelineForm"><div class="crm-grid">${field('Opportunity','name',opp.name,true)}${field('Company','company',opp.company)}${selectField('Stage','stage',opp.stage,STAGES)}${field('Value','value',opp.value,'number')}${field('Probability %','probability',opp.probability??0,'number')}${field('Expected close','expectedClose',opp.expectedClose||'','date')}${field('Owner','owner',opp.owner)}${field('Source','source',opp.source)}${field('Next action','nextAction',opp.nextAction)}${field('Loss reason','lossReason',opp.lossReason||'')}<div class="crm-field full"><label>Notes</label><textarea name="notes">${esc(opp.notes||'')}</textarea></div></div><div class="crm-actions"><button class="crm-btn primary">Save opportunity</button><button class="crm-btn danger" type="button" id="pipelineDelete">Delete</button></div></form><section style="margin-top:22px"><h3 style="font-size:18px">Activity timeline</h3>${events.length?events.map(x=>`<div class="dialer-event"><strong>${esc(x.type)}</strong> · ${esc(x.detail)}<time>${new Date(x.at).toLocaleString()}</time></div>`).join(''):'<div class="crm-empty" style="margin-top:10px;padding:22px">No activity recorded.</div>'}</section></aside></div>`}
  function bindPipeline(){document.querySelectorAll('[data-pipeline-view]').forEach(x=>x.addEventListener('click',()=>{pipelineUI.view=x.dataset.pipelineView;renderPipeline()}));document.querySelectorAll('[data-pipeline-urgency-card]').forEach(x=>x.addEventListener('click',()=>{pipelineUI.urgency=x.dataset.pipelineUrgencyCard;renderPipeline()}));document.getElementById('pipelineOwner')?.addEventListener('change',e=>{pipelineUI.owner=e.target.value;renderPipeline()});document.getElementById('pipelineSource')?.addEventListener('change',e=>{pipelineUI.source=e.target.value;renderPipeline()});document.getElementById('pipelineUrgency')?.addEventListener('change',e=>{pipelineUI.urgency=e.target.value;renderPipeline()});document.querySelectorAll('[data-opp-row]').forEach(x=>x.addEventListener('click',()=>{pipelineUI.selectedId=x.dataset.oppRow;renderPipeline()}));document.getElementById('pipelineAdd')?.addEventListener('click',()=>{const db=load(),opp={id:id('opp'),leadId:null,name:'New solar opportunity',company:'',stage:'New Lead',value:0,probability:10,expectedClose:'',owner:'Unassigned',source:'Manual',nextAction:'Qualify utility bill and roof fit',notes:'',createdAt:now(),updatedAt:now(),stageEnteredAt:now(),history:[]};db.opportunities.unshift(opp);activity(db,'opportunity',opp.id,'created','Residential solar opportunity created manually');save(db);pipelineUI.selectedId=opp.id;renderPipeline()});document.getElementById('pipelineClose')?.addEventListener('click',()=>{pipelineUI.selectedId=null;renderPipeline()});document.getElementById('pipelineForm')?.addEventListener('submit',e=>{e.preventDefault();const db=load(),opp=db.opportunities.find(x=>x.id===pipelineUI.selectedId),data=new FormData(e.target);if(!opp)return;const from=opp.stage,to=String(data.get('stage'));opp.name=String(data.get('name')).trim();opp.company=String(data.get('company')).trim();opp.value=Number(data.get('value'))||0;opp.probability=Math.min(100,Math.max(0,Number(data.get('probability'))||0));opp.expectedClose=String(data.get('expectedClose')||'');opp.owner=String(data.get('owner')).trim();opp.source=String(data.get('source')).trim();opp.nextAction=String(data.get('nextAction')).trim();opp.notes=String(data.get('notes')).trim();opp.updatedAt=now();if(from!==to){opp.stage=to;opp.stageEnteredAt=now();opp.history.unshift({from,to,at:now(),source:'pipeline'});activity(db,'opportunity',opp.id,'stage_change',`${from} → ${to}`);const lead=db.leads.find(x=>x.id===opp.leadId);if(lead){lead.status=to;lead.updatedAt=now();activity(db,'lead',lead.id,'stage_change',`${from} → ${to} from Pipeline`)}}else activity(db,'opportunity',opp.id,'updated','Opportunity fields updated');save(db);renderPipeline()});document.getElementById('pipelineDelete')?.addEventListener('click',()=>{if(!confirm('Delete this opportunity?'))return;const db=load();db.opportunities=db.opportunities.filter(x=>x.id!==pipelineUI.selectedId);activity(db,'opportunity',pipelineUI.selectedId,'deleted','Opportunity deleted');save(db);pipelineUI.selectedId=null;renderPipeline()})}

  function parseSmartLead(raw){
    const text=String(raw||'').trim(), phoneMatch=text.match(/(?:\+?1[\s.-]?)?\(?([2-9]\d{2})\)?[\s.-]?(\d{3})[\s.-]?(\d{4})/), email=text.match(/[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}/)?.[0]||'';
    const first=text.split(/[,\n]/)[0]?.trim()||'', name=/^[A-Za-z][A-Za-z .'-]{2,50}$/.test(first)?first:'', city=text.match(/\b(Bakersfield|Fresno|Los Angeles|Sacramento|San Diego|Phoenix|Las Vegas|Riverside|Stockton)\b/i)?.[0]||'', utility=text.match(/\b(PG&E|SCE|SDG&E|LADWP|APS|NV Energy)\b/i)?.[0]||'', bill=text.match(/\$\s?(\d{2,4})(?:\s*(?:bill|\/mo|monthly))?/i)?.[1]||'', callback=text.match(/(?:call|callback|after)\s+([^,.]+)/i)?.[1]?.trim()||'';
    return {name,phone:phoneMatch?`+1${phoneMatch[1]}${phoneMatch[2]}${phoneMatch[3]}`:'',email,city,utility,bill,homeowner:/\bhomeowner\b/i.test(text)?'Yes':/\brent(?:er|ing)\b/i.test(text)?'No':'Unknown',callback,notes:text,source:'Smart Paste'};
  }
  function installLeadExtensions(){const toolbar=document.querySelector('.page[data-page="leads"] .crm-toolbar');if(!toolbar||document.getElementById('crmSmartPaste'))return;const button=document.createElement('button');button.className='crm-btn';button.id='crmSmartPaste';button.textContent='Smart Paste';toolbar.insertBefore(button,toolbar.querySelector('#crmImportToggle'));button.addEventListener('click',openSmartPaste)}
  function openSmartPaste(){document.getElementById('crmSmartOverlay')?.remove();const wrap=document.createElement('div');wrap.className='crm-overlay';wrap.id='crmSmartOverlay';wrap.innerHTML=`<aside class="crm-drawer" role="dialog" aria-modal="true"><div class="crm-drawer-head"><div><h2>Smart Paste</h2><p>Deterministic local extraction · review required before save</p></div><button class="crm-btn" id="crmSmartClose">Close</button></div><section class="crm-smart"><label>Paste one lead, call note, message, or copied row</label><textarea id="crmSmartRaw" placeholder="David Flores, 661-555-0183, Bakersfield, homeowner, PG&E, $315 bill, interested but call after 5 Thursday"></textarea><div class="crm-actions"><button class="crm-btn primary" id="crmSmartExtract">Extract fields</button></div></section><form id="crmSmartReview" hidden><div id="crmSmartResult"></div><div class="crm-actions"><button class="crm-btn primary" type="submit">Save reviewed lead</button><button class="crm-btn" type="button" id="crmSmartReset">Start over</button></div></form><div class="crm-errors" id="crmSmartError"></div></aside>`;document.body.appendChild(wrap);document.getElementById('crmSmartClose').onclick=()=>wrap.remove();document.getElementById('crmSmartExtract').onclick=()=>{const raw=document.getElementById('crmSmartRaw').value,x=parseSmartLead(raw),db=load(),duplicate=db.leads.find(l=>(x.phone&&l.phone===x.phone)||(x.email&&l.email.toLowerCase()===x.email.toLowerCase()));document.getElementById('crmSmartReview').hidden=false;document.getElementById('crmSmartResult').innerHTML=`<div class="crm-extract-grid">${[['Name','smartName',x.name,'85%'],['Phone','smartPhone',x.phone,x.phone?'98%':'missing'],['Email','smartEmail',x.email,x.email?'98%':'missing'],['City','smartCity',x.city,x.city?'80%':'uncertain'],['Utility','smartUtility',x.utility,x.utility?'90%':'missing'],['Monthly bill','smartBill',x.bill,x.bill?'85%':'missing'],['Homeowner','smartHomeowner',x.homeowner,x.homeowner!=='Unknown'?'90%':'uncertain'],['Callback note','smartCallback',x.callback,x.callback?'72%':'missing'],['Source','smartSource',x.source,'100%']].map(([label,key,value,confidence])=>`<label class="crm-extract-field"><span class="label">${label}</span><input id="${key}" value="${esc(value)}"><span class="crm-confidence">${confidence}</span></label>`).join('')}</div><div class="crm-import" style="margin-top:12px"><b>${duplicate?'Possible duplicate found':'No exact phone/email duplicate found'}</b><p>${duplicate?esc(duplicate.name+' · '+duplicate.phone):'Review all fields before saving.'}</p></div><input type="hidden" id="smartOriginal" value="${esc(raw)}">`;document.getElementById('crmSmartError').textContent=duplicate?'Saving is blocked until the duplicate is resolved or the phone/email is changed.':''};document.getElementById('crmSmartReset').onclick=()=>{document.getElementById('crmSmartReview').hidden=true;document.getElementById('crmSmartResult').innerHTML=''};document.getElementById('crmSmartReview').onsubmit=e=>{e.preventDefault();const db=load(),phone=normalizePhone(document.getElementById('smartPhone').value),email=document.getElementById('smartEmail').value.trim();if(!document.getElementById('smartName').value.trim()){document.getElementById('crmSmartError').textContent='Name is required.';return}if(phone&&!validPhone(phone)){document.getElementById('crmSmartError').textContent='Phone must use E.164 format.';return}if(db.leads.some(l=>(phone&&l.phone===phone)||(email&&l.email.toLowerCase()===email.toLowerCase()))){document.getElementById('crmSmartError').textContent='Duplicate phone or email. Open the existing record instead.';return}const lead={id:id('lead'),name:document.getElementById('smartName').value.trim(),company:'',phone,email,status:'New Lead',priority:'Medium',owner:'',source:document.getElementById('smartSource').value||'Smart Paste',sourceUrl:'',consent:'unknown',consentSource:'',dnc:false,attempts:0,nextAction:document.getElementById('smartCallback').value||'Review and qualify',nextFollowUp:'',estimatedValue:0,notes:document.getElementById('smartOriginal').value,city:document.getElementById('smartCity').value,utility:document.getElementById('smartUtility').value,monthlyBill:Number(document.getElementById('smartBill').value)||null,homeowner:document.getElementById('smartHomeowner').value,createdAt:now(),updatedAt:now()};db.leads.unshift(lead);activity(db,'lead',lead.id,'smart_paste','Lead reviewed and saved from Smart Paste');db.kpiEvents.unshift({id:id('kpi'),type:'lead_created',leadId:lead.id,at:now(),source:'smart_paste',classification:'SIMULATED'});save(db);leadUI.selectedId=lead.id;wrap.remove();renderLeads()}}

  function installAdditionalLeadIntake(){
    const toolbar=document.querySelector('.page[data-page="leads"] .crm-toolbar');
    if(!toolbar||document.getElementById('crmBulkPaste'))return;
    const bulk=document.createElement('button'),imageButton=document.createElement('button');
    bulk.className='crm-btn';bulk.id='crmBulkPaste';bulk.textContent='Bulk Paste';
    imageButton.className='crm-btn';imageButton.id='crmImageImport';imageButton.textContent='Upload image';
    const importToggle=toolbar.querySelector('#crmImportToggle');
    toolbar.insertBefore(bulk,importToggle);
    toolbar.insertBefore(imageButton,importToggle);
    bulk.onclick=openBulkPaste;imageButton.onclick=openImageImport;
  }
  function observeLeadToolbar(){const root=leadPage();if(!root||root.dataset.intakeObserver==='yes')return;root.dataset.intakeObserver='yes';new MutationObserver(()=>{installLeadExtensions();installAdditionalLeadIntake()}).observe(root,{childList:true,subtree:true})}
  function openBulkPaste(){
    const wrap=document.createElement('div');wrap.className='crm-overlay';
    wrap.innerHTML='<aside class="crm-drawer" role="dialog" aria-modal="true"><div class="crm-drawer-head"><div><h2>Bulk Paste</h2><p>Tab, comma, or one-record-per-line intake · review before save</p></div><button class="crm-btn" data-close>Close</button></div><section class="crm-smart"><label>Paste rows as name, phone, source</label><textarea id="crmBulkRaw" placeholder="David Flores,+16615550183,Referral&#10;Jamie Lee,+16615550184,Event"></textarea><div class="crm-actions"><button class="crm-btn primary" id="crmBulkPreview">Preview rows</button></div></section><div id="crmBulkResult"></div><div class="crm-errors" id="crmBulkError"></div></aside>';
    document.body.appendChild(wrap);wrap.querySelector('[data-close]').onclick=()=>wrap.remove();
    wrap.querySelector('#crmBulkPreview').onclick=()=>{
      const db=load(),rows=wrap.querySelector('#crmBulkRaw').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean).map((line,index)=>{const cells=line.split(line.includes('\t')?'\t':',').map(x=>x.trim()),phone=normalizePhone(cells[1]),duplicate=db.leads.some(l=>phone&&l.phone===phone);return{index:index+1,name:cells[0]||'',phone,source:cells[2]||'Bulk Paste',valid:!!cells[0]&&validPhone(phone)&&!duplicate,duplicate}});
      wrap.querySelector('#crmBulkResult').innerHTML='<div class="crm-table-wrap" style="margin-top:14px"><table class="crm-table" style="min-width:0"><thead><tr><th>Row</th><th>Name</th><th>Phone</th><th>Source</th><th>Validation</th></tr></thead><tbody>'+rows.map(r=>'<tr><td>'+r.index+'</td><td>'+esc(r.name)+'</td><td>'+esc(r.phone)+'</td><td>'+esc(r.source)+'</td><td><span class="crm-badge '+(r.valid?'ok':'bad')+'">'+(r.valid?'Ready':r.duplicate?'Duplicate':'Invalid')+'</span></td></tr>').join('')+'</tbody></table></div><div class="crm-actions"><button class="crm-btn primary" id="crmBulkSave" '+(rows.some(r=>r.valid)?'':'disabled')+'>Save '+rows.filter(r=>r.valid).length+' reviewed rows</button></div>';
      wrap.querySelector('#crmBulkSave')?.addEventListener('click',()=>{const current=load(),createdAt=now();let saved=0;for(const row of rows.filter(r=>r.valid)){if(current.leads.some(l=>l.phone===row.phone))continue;const lead={id:id('lead'),name:row.name,company:'',phone:row.phone,email:'',status:'New Lead',priority:'Medium',owner:'',source:row.source,consent:'unknown',consentSource:'',dnc:false,attempts:0,nextAction:'Review and qualify',nextFollowUp:'',estimatedValue:0,notes:'Bulk Paste · operator reviewed',createdAt,updatedAt:createdAt};current.leads.unshift(lead);activity(current,'lead',lead.id,'bulk_paste','Reviewed row saved');saved++}current.imports.unshift({id:id('import'),type:'bulk_paste',imported:saved,rejected:rows.length-saved,createdAt,classification:'SIMULATED'});save(current);wrap.remove();renderLeads()})
    }
  }
  function openImageImport(){
    const wrap=document.createElement('div');wrap.className='crm-overlay';
    wrap.innerHTML='<aside class="crm-drawer" role="dialog" aria-modal="true"><div class="crm-drawer-head"><div><h2>Image lead review</h2><p>Upload and inspect locally before any extraction</p></div><button class="crm-btn" data-close>Close</button></div><div class="dialer-truth"><strong>REQUIRES SETUP</strong><span>Automatic OCR/image extraction needs a configured vision-capable AI provider and server-side upload endpoint. No personal data is sent or saved by this screen.</span></div><div class="crm-field"><label>Select screenshot or photo</label><input id="crmImageFile" type="file" accept="image/png,image/jpeg,image/webp"></div><div id="crmImageReview" class="crm-empty"><h3>No image selected</h3><p>Choose a PNG, JPEG, or WebP to preview it locally. Extraction remains disabled until the required provider is configured.</p></div><div class="crm-actions"><button class="crm-btn" disabled title="Configure a vision-capable AI provider and secure upload endpoint">Extract fields</button><button class="crm-btn" data-open-settings>Open Settings</button></div></aside>';
    document.body.appendChild(wrap);wrap.querySelector('[data-close]').onclick=()=>wrap.remove();wrap.querySelector('[data-open-settings]').onclick=()=>{wrap.remove();document.querySelector('[data-pg="settings"]')?.click()};
    wrap.querySelector('#crmImageFile').onchange=event=>{const file=event.target.files?.[0];if(!file)return;const url=URL.createObjectURL(file),review=wrap.querySelector('#crmImageReview');review.innerHTML='<img alt="Local lead source preview" style="max-width:100%;max-height:420px;border-radius:10px"><p>'+esc(file.name)+' · '+Math.round(file.size/1024)+' KB · local preview only</p>';review.querySelector('img').src=url;review.querySelector('img').onload=()=>URL.revokeObjectURL(url)}
  }
  let callbackView='due';
  function renderCallbacks(){const root=document.querySelector('.page[data-page="callbacks"] .body');if(!root)return;const db=load(),nowMs=Date.now(),items=(db.callbacks||[]).filter(x=>callbackView==='all'||callbackView==='promising'&&['promising','hot'].includes(x.type)||callbackView==='overdue'&&x.status==='open'&&Date.parse(x.dueAt)<nowMs||callbackView==='due'&&x.status==='open'&&Date.parse(x.dueAt)<=nowMs+86400000||callbackView==='completed'&&x.status==='completed');root.innerHTML=`<div class="crm-workspace"><div class="crm-toolbar"><div class="crm-views">${[['due','Due now'],['overdue','Overdue'],['promising','Promising'],['all','Upcoming'],['completed','Completed']].map(([v,l])=>`<button class="crm-btn ${callbackView===v?'active':''}" data-callback-view="${v}">${l}</button>`).join('')}</div><span class="crm-spacer"></span><button class="crm-btn primary" id="callbackAdd">Schedule callback</button></div><div class="crm-metrics">${metric('Open',(db.callbacks||[]).filter(x=>x.status==='open').length,'Action queue')}${metric('Promising',(db.callbacks||[]).filter(x=>x.status==='open'&&x.type==='promising').length,'Priority follow-up')}${metric('Hot',(db.callbacks||[]).filter(x=>x.status==='open'&&x.type==='hot').length,'Immediate attention')}${metric('Overdue',(db.callbacks||[]).filter(x=>x.status==='open'&&Date.parse(x.dueAt)<nowMs).length,'Past due')}${metric('Completed today',(db.callbacks||[]).filter(x=>x.status==='completed'&&new Date(x.completedAt).toDateString()===new Date().toDateString()).length,'Recorded work')}</div><div class="crm-priority-grid">${['hot','promising','standard','confirmation'].map(type=>`<section class="crm-priority-lane"><header><h3>${type[0].toUpperCase()+type.slice(1)}</h3><p>${items.filter(x=>x.type===type).length} callbacks</p></header><div class="crm-priority-body">${items.filter(x=>x.type===type).map(x=>{const lead=db.leads.find(l=>l.id===x.leadId);return`<article class="crm-work-card ${type}"><b>${esc(lead?.name||'Missing lead')}</b><p>${new Date(x.dueAt).toLocaleString()} · ${esc(x.reason)}</p><p>${esc(lead?.phone||'No phone')} · attempts ${lead?.attempts||0}</p><div class="crm-actions"><button class="crm-btn" data-callback-call="${x.id}">Call now</button><button class="crm-btn" data-callback-snooze="${x.id}">Snooze</button><button class="crm-btn" data-callback-complete="${x.id}">Complete</button></div></article>`}).join('')||'<div class="crm-empty" style="padding:18px">No callbacks</div>'}</div></section>`).join('')}</div></div>`;bindCallbacks()}
  function callbackDialog(){const db=load();if(!db.leads.length){alert('Add a lead before scheduling a callback.');return}const wrap=document.createElement('div');wrap.className='crm-overlay';wrap.innerHTML=`<aside class="crm-drawer"><div class="crm-drawer-head"><h2>Schedule callback</h2><button class="crm-btn" id="callbackClose">Close</button></div><form id="callbackForm"><div class="crm-grid">${selectField('Lead','leadId','',db.leads.map(x=>x.id))}${selectField('Type','type','promising',['standard','promising','hot','confirmation','no-show recovery','proposal follow-up','contract follow-up'])}${field('Due','dueAt','', 'datetime-local')}${field('Reason','reason','')}${field('Assigned user','assignedUser','Unassigned')}<div class="crm-field full"><label>Notes</label><textarea name="notes"></textarea></div></div><button class="crm-btn primary">Save callback</button></form></aside>`;document.body.appendChild(wrap);const leadSelect=wrap.querySelector('[name="leadId"]');[...leadSelect.options].forEach((o,i)=>o.textContent=db.leads[i]?.name||o.value);wrap.querySelector('#callbackClose').onclick=()=>wrap.remove();wrap.querySelector('#callbackForm').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target),dueAt=String(data.get('dueAt'));if(!dueAt){return}const item={id:id('callback'),leadId:String(data.get('leadId')),type:String(data.get('type')),dueAt,reason:String(data.get('reason'))||'Follow up',assignedUser:String(data.get('assignedUser')),notes:String(data.get('notes')),status:'open',createdAt:now()};db.callbacks.unshift(item);activity(db,'lead',item.leadId,'callback_scheduled',`${item.type} callback · ${item.dueAt}`);db.kpiEvents.unshift({id:id('kpi'),type:item.type==='promising'?'promising_callback_scheduled':'callback_scheduled',leadId:item.leadId,callbackId:item.id,at:now(),classification:'SIMULATED'});const lead=db.leads.find(x=>x.id===item.leadId);if(lead){lead.nextFollowUp=dueAt;lead.nextAction=item.reason;if(item.type==='promising')lead.status='Promising Callback'}save(db);wrap.remove();renderCallbacks()}}
  function bindCallbacks(){document.querySelectorAll('[data-callback-view]').forEach(x=>x.onclick=()=>{callbackView=x.dataset.callbackView;renderCallbacks()});document.getElementById('callbackAdd')?.addEventListener('click',callbackDialog);document.querySelectorAll('[data-callback-complete]').forEach(x=>x.onclick=()=>{const db=load(),item=db.callbacks.find(c=>c.id===x.dataset.callbackComplete);item.status='completed';item.completedAt=now();activity(db,'lead',item.leadId,'callback_completed',item.reason);db.kpiEvents.unshift({id:id('kpi'),type:'callback_completed',leadId:item.leadId,callbackId:item.id,at:now(),classification:'SIMULATED'});save(db);renderCallbacks()});document.querySelectorAll('[data-callback-snooze]').forEach(x=>x.onclick=()=>{const db=load(),item=db.callbacks.find(c=>c.id===x.dataset.callbackSnooze);item.dueAt=new Date(Date.now()+86400000).toISOString();activity(db,'lead',item.leadId,'callback_snoozed','Snoozed 24 hours');save(db);renderCallbacks()});document.querySelectorAll('[data-callback-call]').forEach(x=>x.onclick=()=>{const db=load(),item=db.callbacks.find(c=>c.id===x.dataset.callbackCall),lead=db.leads.find(l=>l.id===item.leadId);if(lead)addToDialer(lead);document.querySelector('[data-pg="dialer"]')?.click()})}

  let appointmentView='agenda';
  function renderAppointments(){const root=document.querySelector('.page[data-page="appointments"] .body');if(!root)return;const db=load(),items=(db.appointments||[]).filter(x=>appointmentView==='all'||appointmentView==='agenda'&&!['cancelled'].includes(x.status)||x.status===appointmentView);root.innerHTML=`<div class="crm-workspace"><div class="crm-toolbar"><div class="crm-views">${[['agenda','Agenda'],['needs confirmation','Needs confirmation'],['confirmed','Confirmed'],['completed','Completed'],['no-show','No-show'],['all','All']].map(([v,l])=>`<button class="crm-btn ${appointmentView===v?'active':''}" data-appointment-view="${v}">${l}</button>`).join('')}</div><span class="crm-spacer"></span><button class="crm-btn primary" id="appointmentAdd">Book appointment</button></div><div class="crm-metrics">${metric('Upcoming',(db.appointments||[]).filter(x=>Date.parse(x.startsAt)>Date.now()&&!['cancelled'].includes(x.status)).length,'Scheduled')}${metric('Needs confirmation',(db.appointments||[]).filter(x=>x.status==='needs confirmation').length,'Follow up')}${metric('Confirmed',(db.appointments||[]).filter(x=>x.status==='confirmed').length,'Ready')}${metric('Sat',(db.appointments||[]).filter(x=>x.status==='completed').length,'Completed')}${metric('No-show',(db.appointments||[]).filter(x=>x.status==='no-show').length,'Recovery')}</div><div class="crm-agenda">${items.sort((a,b)=>Date.parse(a.startsAt)-Date.parse(b.startsAt)).map(x=>{const lead=db.leads.find(l=>l.id===x.leadId);return`<article class="crm-agenda-row"><div class="crm-agenda-time">${new Date(x.startsAt).toLocaleString([], {month:'short',day:'numeric',hour:'numeric',minute:'2-digit'})}</div><div><b>${esc(lead?.name||'Missing lead')}</b><p>${esc(x.type)} · ${esc(x.address||x.meetingLink||'Location pending')}</p></div><div><span class="crm-badge ${x.status==='confirmed'?'ok':x.status==='no-show'?'bad':'warn'}">${esc(x.status)}</span><p>${esc(x.closer||'Closer unassigned')}</p></div><div class="crm-actions"><button class="crm-btn" data-appointment-status="${x.id}:confirmed">Confirm</button><button class="crm-btn" data-appointment-status="${x.id}:completed">Mark sat</button><button class="crm-btn" data-appointment-status="${x.id}:no-show">No-show</button></div></article>`}).join('')||'<div class="crm-empty"><h3>No appointments in this view</h3><p>Book one from an eligible lead. Calendar delivery remains Requires Setup.</p></div>'}</div></div>`;bindAppointments()}
  function appointmentDialog(leadId=''){const db=load();if(!db.leads.length){alert('Add a lead before booking an appointment.');return}const wrap=document.createElement('div');wrap.className='crm-overlay';wrap.innerHTML=`<aside class="crm-drawer"><div class="crm-drawer-head"><h2>Book appointment</h2><button class="crm-btn" id="appointmentClose">Close</button></div><form id="appointmentForm"><div class="crm-grid">${selectField('Lead','leadId',leadId,db.leads.map(x=>x.id))}${field('Starts','startsAt','', 'datetime-local')}${selectField('Type','type','Solar consultation',['Solar consultation','Site visit','Proposal review','Contract review','Custom'])}${field('Setter','setter','Unassigned')}${field('Closer','closer','Unassigned')}${field('Address','address','')}${field('Meeting link','meetingLink','url')}${field('Utility','utility','')}${field('Monthly bill','monthlyBill','', 'number')}<div class="crm-field full"><label>Notes</label><textarea name="notes"></textarea></div></div><button class="crm-btn primary">Book locally</button><p class="crm-sub">No calendar invitation or message will be sent. Calendar OAuth is not configured.</p></form></aside>`;document.body.appendChild(wrap);const select=wrap.querySelector('[name="leadId"]');[...select.options].forEach((o,i)=>o.textContent=db.leads[i]?.name||o.value);wrap.querySelector('#appointmentClose').onclick=()=>wrap.remove();wrap.querySelector('#appointmentForm').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target),startsAt=String(data.get('startsAt'));if(!startsAt)return;const item={id:id('appointment'),leadId:String(data.get('leadId')),startsAt,type:String(data.get('type')),setter:String(data.get('setter')),closer:String(data.get('closer')),address:String(data.get('address')),meetingLink:String(data.get('meetingLink')),utility:String(data.get('utility')),monthlyBill:Number(data.get('monthlyBill'))||null,notes:String(data.get('notes')),status:'needs confirmation',createdAt:now()};db.appointments.unshift(item);const lead=db.leads.find(x=>x.id===item.leadId);if(lead){lead.status='Appointment Set';lead.appointmentId=item.id;lead.nextAction='Confirm appointment'}const opp=lead&&ensureOpportunity(db,lead);if(opp){opp.stage='Appointment Set';opp.updatedAt=now();opp.history.unshift({from:opp.stage,to:'Appointment Set',at:now(),source:'appointment'})}activity(db,'lead',item.leadId,'appointment_set',item.startsAt);db.kpiEvents.unshift({id:id('kpi'),type:'appointment_set',leadId:item.leadId,appointmentId:item.id,at:now(),classification:'SIMULATED'});save(db);wrap.remove();renderAppointments()}}
  function bindAppointments(){document.querySelectorAll('[data-appointment-view]').forEach(x=>x.onclick=()=>{appointmentView=x.dataset.appointmentView;renderAppointments()});document.getElementById('appointmentAdd')?.addEventListener('click',()=>appointmentDialog());document.querySelectorAll('[data-appointment-status]').forEach(x=>x.onclick=()=>{const [itemId,status]=x.dataset.appointmentStatus.split(':'),db=load(),item=db.appointments.find(a=>a.id===itemId);if(!item)return;const prior=item.status;item.status=status;item.updatedAt=now();const lead=db.leads.find(l=>l.id===item.leadId);if(lead)lead.status=status==='confirmed'?'Appointment Confirmed':status==='completed'?'Appointment Sat':lead.status;activity(db,'lead',item.leadId,'appointment_status',`${prior} → ${status}`);db.kpiEvents.unshift({id:id('kpi'),type:status==='confirmed'?'appointment_confirmed':status==='completed'?'appointment_sat':'appointment_no_show',leadId:item.leadId,appointmentId:item.id,at:now(),classification:'SIMULATED'});save(db);renderAppointments()})}

  function renderKpi(){const root=document.querySelector('.page[data-page="kpi"] .body');if(!root)return;const db=load(),dialer=(()=>{try{return JSON.parse(localStorage.getItem('conduitDialerV1')||'{}')}catch{return{}}})(),events=[...(db.kpiEvents||[])],count=t=>events.filter(x=>x.type===t).length,attempts=Number(dialer.attempts)||events.filter(x=>x.type==='dial_attempt').length,connected=Number(dialer.reached)||count('connected_call'),appointments=count('appointment_set'),callbacks=count('callback_scheduled')+count('promising_callback_scheduled'),calls=(dialer.events||[]).filter(x=>x.type==='disposition');root.innerHTML=`<div class="crm-workspace"><div class="crm-toolbar"><b>Today · local normalized events</b><span class="crm-spacer"></span><button class="crm-btn" id="kpiExport">Export events</button></div><div class="kpi-grid">${[['Dial attempts',attempts,'Recorded sessions'],['Connected',connected,attempts?Math.round(connected/attempts*100)+'% contact rate':'0% contact rate'],['Callbacks',callbacks,count('promising_callback_scheduled')+' promising'],['Appointments',appointments,attempts?Math.round(appointments/attempts*100)+'% set rate':'0% set rate'],['Qualified',count('qualified_lead'),'Recorded outcomes'],['Confirmed',count('appointment_confirmed'),'Appointments'],['Sat',count('appointment_sat'),'Appointments'],['No-show',count('appointment_no_show'),'Recovery required']].map(([l,v,d])=>`<div class="kpi-card"><div class="l">${l}</div><div class="v">${v}</div><div class="d">${d}</div></div>`).join('')}</div><section class="crm-table-wrap"><table class="crm-call-log"><thead><tr><th>Timestamp</th><th>Lead</th><th>State / disposition</th><th>Duration</th><th>Callback</th><th>Notes</th><th>Mode</th></tr></thead><tbody>${calls.map(c=>{const lead=(dialer.leads||[]).find(l=>l.id===c.leadId)||db.leads.find(l=>`crm-${l.id}`===c.leadId);return`<tr><td>${new Date(c.at).toLocaleString()}</td><td>${esc(lead?.name||'Unknown')}</td><td>${esc(c.detail)}</td><td>${c.durationSeconds||0}s</td><td>${esc(lead?.callbackAt||'—')}</td><td>${esc(lead?.notes||'—')}</td><td>SIMULATED</td></tr>`}).join('')||'<tr><td colspan="7">No recorded call dispositions.</td></tr>'}</tbody></table></section></div>`;document.getElementById('kpiExport')?.addEventListener('click',()=>{const url=URL.createObjectURL(new Blob([JSON.stringify({exportedAt:now(),events,calls},null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='conduit-kpi-events.json';a.click();URL.revokeObjectURL(url)})}

  function installAgent(){if(window.CONDUIT_CRM_AGENT_DISABLED||document.getElementById('conduitAgentLauncher'))return;const launch=document.createElement('button');launch.className='agent-launcher';launch.id='conduitAgentLauncher';launch.textContent='Conduit Agent';const panel=document.createElement('aside');panel.className='agent-panel';panel.id='conduitAgentPanel';panel.innerHTML=`<div class="crm-drawer-head"><div><h2>Conduit Agent</h2><p>Permission-aware local CRM tools</p></div><button class="crm-btn" id="agentClose">Close</button></div><div class="dialer-truth"><strong>SIMULATED</strong><span>Reads local CRM directly. Write actions require confirmation. No external model is invoked.</span></div><div class="crm-field"><label>Lead context</label><select id="agentLead"></select></div><div class="agent-thread" id="agentThread"><div class="agent-message">Ask for a lead summary, next action, due callbacks, or stalled opportunities.</div></div><div class="crm-field"><label>Command</label><textarea id="agentInput" rows="4" placeholder="Summarize this lead"></textarea></div><div class="crm-actions"><button class="crm-btn primary" id="agentRun">Run read action</button><button class="crm-btn" id="agentPreviewCallback">Preview callback write</button></div>`;document.body.append(launch,panel);const refresh=()=>{const db=load(),select=document.getElementById('agentLead');select.innerHTML=db.leads.map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('')||'<option value="">No leads</option>'};refresh();launch.onclick=()=>{panel.classList.add('open');localStorage.setItem('conduit.agent.open','yes');refresh()};document.getElementById('agentClose').onclick=()=>{panel.classList.remove('open');localStorage.setItem('conduit.agent.open','no')};document.getElementById('agentRun').onclick=()=>{const db=load(),lead=db.leads.find(x=>x.id===document.getElementById('agentLead').value),q=document.getElementById('agentInput').value.toLowerCase(),thread=document.getElementById('agentThread');let answer;if(!lead)answer='No lead is selected.';else if(q.includes('next'))answer=`Next action for ${lead.name}: ${lead.nextAction||'complete qualification and set a follow-up'}. Status ${lead.status}; attempts ${lead.attempts||0}.`;else answer=`${lead.name} · ${lead.phone||'no phone'} · ${lead.status}. Source ${lead.source||'unknown'}; consent ${lead.consent}; DNC ${lead.dnc?'yes':'no'}; utility ${lead.utility||'unknown'}; monthly bill ${lead.monthlyBill?money(lead.monthlyBill):'unknown'}; next ${lead.nextAction||'not set'}.`;thread.innerHTML+=`<div class="agent-message">${esc(answer)}</div>`;db.agentAudit.unshift({id:id('agent'),type:'read',leadId:lead?.id||null,command:q,at:now(),status:'success'});save(db)};document.getElementById('agentPreviewCallback').onclick=()=>{const leadId=document.getElementById('agentLead').value,thread=document.getElementById('agentThread');thread.innerHTML+=`<div class="agent-message agent-preview"><b>Write preview</b><br>Create a promising callback for tomorrow for the selected lead. Nothing has been written.<div class="crm-actions"><button class="crm-btn primary" id="agentConfirmWrite">Confirm write</button></div></div>`;document.getElementById('agentConfirmWrite').onclick=()=>{const db=load(),item={id:id('callback'),leadId,type:'promising',dueAt:new Date(Date.now()+86400000).toISOString(),reason:'Agent-created promising follow-up',assignedUser:'Unassigned',notes:'Confirmed by operator',status:'open',createdAt:now()};db.callbacks.unshift(item);db.agentAudit.unshift({id:id('agent'),type:'write',leadId,command:'create promising callback',at:now(),status:'confirmed'});activity(db,'lead',leadId,'callback_scheduled','Promising callback created by confirmed agent action');save(db);document.getElementById('agentConfirmWrite').disabled=true;document.getElementById('agentConfirmWrite').textContent='Written after confirmation'}};if(localStorage.getItem('conduit.agent.open')==='yes')panel.classList.add('open');document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='j'){e.preventDefault();launch.click()}})}
  function syncDialerKpi(){try{const dialer=JSON.parse(localStorage.getItem(DIALER_KEY)||'{}'),db=load(),known=new Set((db.kpiEvents||[]).map(x=>x.sourceEventId));let added=0;for(const event of dialer.events||[]){if(known.has(event.id))continue;let type='';if(event.type==='call'&&/dialing state entered/i.test(event.detail))type='dial_attempt';else if(event.type==='call'&&/connected state entered/i.test(event.detail))type='connected_call';else if(event.type==='disposition'){if(/^qualified/i.test(event.detail))type='qualified_lead';else if(/^appointment-set/i.test(event.detail))type='appointment_set';else type='call_disposition'}if(!type)continue;db.kpiEvents.unshift({id:id('kpi'),type,leadId:event.leadId,at:event.at,sourceEventId:event.id,classification:'SIMULATED'});added++}if(added)save(db)}catch{}}
  function bindDialerKpi(){['dialerCallButton','dialerSaveOutcome'].forEach(buttonId=>document.getElementById(buttonId)?.addEventListener('click',()=>setTimeout(syncDialerKpi,1200)))}
  function installSmartPasteOriginalPreview(){document.addEventListener('click',event=>{if(!event.target.closest('#crmSmartExtract'))return;setTimeout(()=>{const result=document.getElementById('crmSmartResult'),raw=document.getElementById('crmSmartRaw')?.value;if(!result||!raw||result.querySelector('.crm-original-preview'))return;const preview=document.createElement('details');preview.className='crm-import crm-original-preview';preview.open=true;preview.innerHTML=`<summary><b>Original pasted text</b></summary><p>${esc(raw)}</p>`;result.appendChild(preview)},0)})}
  function enforceAgentShortcutSetting(){document.addEventListener('keydown',event=>{if(workflowPrefs().agentShortcut==='disabled'&&(event.metaKey||event.ctrlKey)&&event.key.toLowerCase()==='j'){event.preventDefault();event.stopImmediatePropagation()}},true)}
  const workflowDefaults={callbackType:'promising',appointmentType:'Solar consultation',setter:'Unassigned',closer:'Unassigned',dialTarget:100,agentShortcut:'enabled',agentWrites:'confirm'};
  function workflowPrefs(){try{return{...workflowDefaults,...JSON.parse(localStorage.getItem('conduit.workflow.settings')||'{}')}}catch{return{...workflowDefaults}}}
  function installWorkflowSettings(){const ids={setWorkflowCallbackType:'callbackType',setWorkflowAppointmentType:'appointmentType',setWorkflowSetter:'setter',setWorkflowCloser:'closer',setWorkflowDialTarget:'dialTarget',setWorkflowAgentShortcut:'agentShortcut',setWorkflowAgentWrites:'agentWrites'},prefs=workflowPrefs();for(const [nodeId,key] of Object.entries(ids)){const node=document.getElementById(nodeId);if(node)node.value=String(prefs[key])}document.getElementById('settingsWorkflowSave')?.addEventListener('click',()=>{const next={};for(const [nodeId,key] of Object.entries(ids)){next[key]=document.getElementById(nodeId)?.value||workflowDefaults[key]}next.dialTarget=Number(next.dialTarget)||0;localStorage.setItem('conduit.workflow.settings',JSON.stringify(next));document.getElementById('settingsWorkflowStatus').textContent=`Saved locally at ${new Date().toLocaleString()}. Callback, appointment, KPI target, and agent shortcut defaults are active.`});document.addEventListener('click',event=>{const prefs=workflowPrefs();if(event.target.closest('#callbackAdd'))setTimeout(()=>{const node=document.querySelector('#callbackForm [name="type"]');if(node)node.value=prefs.callbackType},0);if(event.target.closest('#appointmentAdd'))setTimeout(()=>{const form=document.getElementById('appointmentForm');if(!form)return;form.querySelector('[name="type"]').value=prefs.appointmentType;form.querySelector('[name="setter"]').value=prefs.setter;form.querySelector('[name="closer"]').value=prefs.closer},0)})}
  function removeRepeatedVisuals(){document.querySelectorAll('.page[data-page="leads"] .visual-system.upgraded,.page[data-page="pipeline"] .visual-system.upgraded,.page[data-page="leads"] .section-performance-chart,.page[data-page="pipeline"] .section-performance-chart,.page[data-page="leads"] .ai-staged-records,.page[data-page="pipeline"] .ai-staged-records,#dialerPipelineRecords').forEach(x=>x.remove())}
  function installStarterNotices(){const db=load();document.querySelectorAll('.page[data-page="leads"] .crm-workspace,.page[data-page="callbacks"] .crm-workspace,.page[data-page="appointments"] .crm-workspace,.page[data-page="pipeline"] .crm-workspace,.page[data-page="kpi"] .crm-workspace').forEach(root=>{root.querySelector('.crm-starter-notice')?.remove();if(!db.starterWorkspace)return;const notice=document.createElement('div');notice.className='crm-starter-notice';notice.innerHTML='<div><b>LOCAL STARTER WORKSPACE</b><span>Example records connect Leads → Callbacks → Appointments → Pipeline → KPI. No calls, bookings, outreach, or revenue actually occurred.</span></div><button class="crm-btn" data-clear-starter>Clear starter records</button>';root.prepend(notice)});document.querySelectorAll('[data-clear-starter]').forEach(button=>button.addEventListener('click',()=>{if(!confirm('Clear all labeled Conduit starter records?'))return;save(emptyDB());renderAll()}))}
  function renderAll(){renderLeads();installLeadExtensions();installAdditionalLeadIntake();renderCallbacks();renderAppointments();renderPipeline();renderKpi();installStarterNotices();removeRepeatedVisuals()}
  function boot(){removeRepeatedVisuals();renderAll();observeLeadToolbar();enforceAgentShortcutSetting();installAgent();bindDialerKpi();installSmartPasteOriginalPreview();installWorkflowSettings();setTimeout(()=>{removeRepeatedVisuals();installLeadExtensions();installAdditionalLeadIntake()},350);setTimeout(()=>{removeRepeatedVisuals();installAdditionalLeadIntake()},900);window.addEventListener('conduit:crm-updated',()=>setTimeout(renderAll,0));window.addEventListener('storage',e=>{if(e.key===DB_KEY)renderAll()})}
  window.conduitCRM={load,save,addToDialer,ensureOpportunity,renderLeads,renderCallbacks,renderAppointments,renderPipeline,renderKpi,parseSmartLead,appointmentDialog};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
